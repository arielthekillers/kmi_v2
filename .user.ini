; ── PHP Upload Limits ─────────────────────────────────────────────────
; This file works on PHP-FPM hosting (cPanel, Nginx, etc.)
; .htaccess php_value works on Apache+mod_php — this covers the other case.
upload_max_filesize = 32M
post_max_size = 32M
memory_limit = 128M
max_execution_time = 120
